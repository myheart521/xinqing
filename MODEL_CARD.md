# 心理健康问答模型卡

Built with Qwen.

本卡记录历史 Qwen V2 微调产物。**当前公开包不含该模型权重**：训练混合数据的授权范围、私人参考材料的处理依据，以及模型记忆泄露风险尚未得到完整验证。公开的 114,900 条合成子集也不能用于声称完整复现了原 200,000 条训练运行。

## 模型与训练

| 项目 | 已核实信息 |
| --- | --- |
| 基座 | [Qwen/Qwen2.5-3B-Instruct](https://huggingface.co/Qwen/Qwen2.5-3B-Instruct) |
| 架构 | Qwen2ForCausalLM，36 层，hidden size 2048，16 个注意力头，2 个 KV 头 |
| 微调形式 | PEFT LoRA，CAUSAL_LM |
| LoRA | r=32，alpha=64，dropout=0.05，bias=none |
| 目标模块 | q_proj、k_proj、v_proj、o_proj、gate_proj、up_proj、down_proj |
| 适配器参数量 | 59,867,136（由配置对应矩阵维度推算） |
| 历史适配器文件 | adapter_model.safetensors，239,536,272 字节 |
| 历史适配器 SHA-256 | `3c5981ef4b98f7003c62f4d220c35448f9d9eae8773d33964bf18ef9b2ec9731` |
| 训练集 / 验证集 / 测试集 | 200,000 / 200 / 400 条，来自历史运行记录 |
| 训练配置记录 | 2 epoch，6,250 step，4 张 A100，有效 batch size 64，学习率 5e-5，最大序列长度 1,024，bf16 |

架构和 LoRA 参数来自保存的 JSON 配置；训练超参数来自归档参数表。未重新运行训练，也未反序列化 training_args.bin。适配器记录中的机器专用路径不在本公开包中。

## 训练数据

200,000 条训练样本包括 114,900 条可由静态模板独立生成的样本、3,100 条旧版自建样本，以及 82,000 条公开来源转换样本。其中 CPsyCounD 为 20,552 条，Psy-Insight 为 1,699 条，SMILE 为 59,749 条。

旧版材料记载 300 条私人聊天参考生成的合成改写，并声称未保留原文；本次没有取得完整同意记录或针对这些参考材料的记忆泄露评估。其他公开来源还涉及各自的数据许可与原始语料权利。详见 [DATASET_CARD.md](DATASET_CARD.md)。

## 已保存的评估结果及限制

历史 train_results.json 记录 train loss 为 0.899543，训练耗时 37,763.378 秒；eval_results.json 记录 200 条验证样本的 eval loss 为 0.139233。以上为保存的训练指标，不是独立临床评估，也不是本次重新测试结果。

训练脚本使用 DataCollatorForLanguageModeling(mlm=False)，未实施仅助手回答计算损失的掩码。公开模板包含较多重复的回答结构；较低 loss 不能证明真实咨询能力。未获得充分的独立临床效果、危机处置可靠性、跨人群公平性或隐私提取测试证据。

## 使用边界

研究目标是中文校园心理支持话术、拒答边界和工具调用格式学习。模型不能作为医学诊断、治疗决策、药物建议或自动危机处置系统。历史工具样本的部分参数与用户问题独立抽样，可能存在语义不一致，不能据此认为模型能够可靠执行真实预约或转接。

## 许可与发布状态

基座随附的是 **Qwen RESEARCH LICENSE AGREEMENT（2024-09-19）**，仅授予非商业研究和评估用途；它不是 Apache-2.0 或 MIT 许可证。商业使用需按上游条款另行申请。任何未来适配器发布均须保留适用的 Qwen 许可、归属声明、修改说明及 Built with Qwen 标识，并完成训练数据权利与隐私核查。

代码仓库的许可证不覆盖 Qwen 基座、历史模型权重或第三方训练语料。当前发布不提供已训练适配器下载，也不声称已完成模型隐私审计。
