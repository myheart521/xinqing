# 合成校园心理支持数据集

本次发布 **114,900 条由静态模板和固定随机种子生成的中文样本**。发布文件不包含历史混合数据集、第三方咨询语料、聊天导出、私人聊天参考改写、真实用户测评或咨询记录。

## 文件与复现

| 项目 | 内容 |
| --- | --- |
| 发布文件 | `psych-health-synthetic-114900.jsonl.gz` |
| 解压格式 | UTF-8 JSONL；每行一个 messages 对象，两轮 user / assistant |
| 解压大小 | 59,016,611 字节 |
| 样本数 | 114,900 |
| 解压文件 SHA-256 | `34636fac3d2240ddd5fca8494cc1831fc44dc2212d36fbf7621eaed47b2d322c` |
| 构建方式 | 静态通用场景模板组合；seed=20260511；无外部数据、网络调用或模型生成 |
| 验证环境 | Python 3.12.7；生成结果与历史独立新增子集逐字节相同 |
| 划分 | 仅提供历史训练子集，不提供未经审查的旧版 dev/test |

```bash
python scripts/generate_synthetic_dataset.py --output train.synthetic.jsonl
```

生成器拒绝覆盖已有文件，并核对固定 SHA-256。为保持历史字节一致，输出采用 CRLF 换行。压缩包与生成器输出内容一致；压缩文件哈希见 `release_manifest.json`。

## 构成与来源证据

| 类型 | 条数 |
| --- | ---: |
| 校园场景支持 | 48,100 |
| 危机拒答与安全边界 | 27,500 |
| 工具调用示例 | 29,500 |
| 服务流程 FAQ | 9,800 |
| 合计 | 114,900 |

本次检查了生成函数和所有静态模板，将其从混合数据构建脚本中提取为独立生成器，删除旧数据加载、目录复制和删除、私人参考处理及机器专用路径。生成器从零生成的每条记录、排列顺序和文件字节均与历史 `train_new_only.jsonl` 相同。这一可复现来源链是本子集的隐私依据；标识符模式扫描仅为辅助检查。

## 质量与适用范围

这些是模板数据，不能代表真实学生、真实咨询过程或临床分布。情绪和风险标签来源于模板规则，未经专业诊断；回答具有重复性。原始元数据对危机场景标注需要完整人工审核，对服务和工具样本标注需要业务审核。本次发布没有宣称这些审核已完成。

**已知问题：工具请求文字和工具参数采用独立随机抽样，可能出现预约动作、量表类型等不匹配。** 数据用于研究构建流程与复现历史实验；在训练真实工具执行能力前，应修正并单独评估这些问题。发布保留历史记录，不将其描述为生产级或临床验证数据。

建议按模板族、场景和回答模式分组划分后续评测，避免同源模板泄露造成虚高结果。不得将本子集的训练结果表述为完整原模型的复现，也不能用合成标签作为对真实用户的临床诊断依据。

## 未发布的历史数据

| 历史来源 | 已知情况 | 本次处理 |
| --- | --- | --- |
| 旧版自建 3,100 条 | 含私人聊天参考的合成改写记录；缺少完整同意及不可追溯性审查证据 | 不发布 |
| CPsyCounD 20,552 条转换样本 | 上游卡标注 CC-BY-SA-4.0；需遵守署名、共享方式和相关数据要求 | 不重新分发，提供上游链接 |
| SMILE 59,749 条转换样本 | 仓库 LICENSE 为 CC0；介绍称由真实心理互助 QA 经模型扩展 | 未仅因 CC0 就断言不存在隐私风险；不重新分发 |
| Psy-Insight 1,699 条转换样本 | 上游说明涵盖多个原始来源和不同条款，含付费语料、抓取内容和书籍材料 | 尚未逐项核实再分发权利；不重新分发 |

上游资料：[CPsyCounD](https://huggingface.co/datasets/CAS-SIAT-XinHai/CPsyCoun)、[SMILE](https://github.com/qiuhuachuan/SMILE)、[Psy-Insight 来源说明](https://github.com/ckqqqq/Psy-Insight/blob/main/docs/data_source/data_source.md)。公开可下载不等于可以把不同来源统一改为仓库代码许可证。

## 许可

本项目的模板生成子集采用 **CC BY 4.0**，见 `LICENSE-DATA`；复用时请注明“心理健康项目贡献者，合成校园心理支持数据集，114,900 条版本”，并标明修改。该许可仅适用于本次合成子集，不扩展到历史混合数据、第三方语料、任何模型权重或基座模型。
