# 本次发布验证

以下记录对应公开源码整理，不代表对真实业务或临床效果的验收。

| 检查 | 结果与范围 |
| --- | --- |
| 后端 `mvn -o -DskipTests package` | 4 个 Maven 模块编译打包通过；未运行依赖外部服务的集成测试 |
| 教师端 `npm run pure-build` | Vite 生产打包通过；存在较大分块提示 |
| 教师端 `npm run build` | 严格 TypeScript 检查未通过，记录到 227 条 TS 诊断；涉及旧源码类型声明、缺失类型依赖及配置类型等。没有通过关闭检查将其描述为通过 |
| 学生端/鸿蒙端 | JSON/JSON5、相对导入、页面清单、资源引用及 Vue/JS/TS 语法检查通过；没有运行 HBuilderX/DevEco 整包构建和真机测试 |
| 静态凭据扫描 | 最终准备提交文件的 Gitleaks 扫描为 0；另对私人地址、身份信息、密钥文件、SQL 数据和 Git 文件范围进行检查 |
| 独立代码复核 | 后端、教师端、数字人目录未发现已知静态凭据/私人身份信息发布阻塞；已移除登录 DTO 与 token 日志、旧默认账户绕过和未核实的热线占位值 |
| 数据集 | 114,900 行 JSONL 可解析；独立生成结果与历史合成子集逐字节一致；发布压缩文件解压后的 SHA-256 再次核对通过 |
| 演示素材 | 选定片段按 0.5 秒间隔采样目检；移除音轨，网页演示裁去浏览器和账号区域。图片元数据未见身份字段 |

## 尚未完成的运行验证

没有使用原数据库、Redis、OSS、邮件或模型服务进行端到端测试。空库不提供账户、角色菜单及业务种子数据；平台签名和外部服务授权需使用者自行配置。语音/数字人/PPT 等集成存在公开版边界和占位模块。移动端原素材改为中性资源，界面与历史演示不同。

历史模型权重未发布，不能用公开合成子集声称完整复现了混合训练实验。所有历史训练指标均注明来源和适用限制。

本次为脱敏源码与材料归档。后续生产部署、严格类型修复、业务权限验证和模型效果评估属于需要另行执行的工作。
